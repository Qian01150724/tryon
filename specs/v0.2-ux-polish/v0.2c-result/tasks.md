# Tasks

> 来源：specs/v0.2-ux-polish/v0.2c-result/plan.md
> 规则：按顺序执行，未通过验证不得进入下一个 task。
> UI 类任务实施前必读：`components/ResultDisplay.tsx` 有结果分支（代码即视觉真相）

## Task #1: 验证前后对比滑动条

- **目标**：确认对比滑动条在桌面端和移动端正确运行
- **改动文件**：无（功能已实现）
- **视觉参考**：
  - 底层换装结果图 + 上层原图 clipPath 裁切
  - 白色分割线 + ↔ 圆形手柄，跟随 sliderPos
  - 左上"原图"标签（bg-black/40）、右上"换装后"标签（bg-purple-500/80）
- **验证**：
  - 命令：`pnpm dev`，上传人物图 + 服装图，点击生成，等待结果
  - 结果出现后，对比区默认分割线在 50% 居中
  - 左右拖动滑条，原图和结果图随分割线比例正确显示，无图层错位
  - 两侧标签"原图"和"换装后"清晰可见
  - 移动端（或 DevTools 模拟）触摸拖动也能正常工作
- **对应**：plan §5 / FR-001, SC-001, SC-002

---

## Task #2: 验证全屏放大弹窗

- **目标**：确认全屏弹窗正确打开/关闭，无层级问题
- **改动文件**：无（功能已实现）
- **视觉参考**：
  - 对比区右下角 ⛶ 按钮（bg-black/40，w-8 h-8）
  - 弹窗：fixed inset-0 bg-black/85 z-50，结果图居中 max-w-full max-h-full
  - 右上角 ✕ 关闭按钮（w-9 h-9 bg-white/20）
- **验证**：
  - 点击右下角 ⛶ → 全屏弹窗出现，结果图居中，不超出屏幕边缘
  - 点击弹窗遮罩（非图片区域）→ 弹窗关闭
  - 点击图片本身 → 弹窗不关闭（stopPropagation）
  - 点击右上角 ✕ → 弹窗关闭
  - 关闭后再次点击 ⛶ → 弹窗可再次打开（isZoomed 正确重置）
  - SC-004：若 personImage 为 null（可临时在代码里注释掉 personImage prop 测试）→ 结果图直接展示，点击触发放大
- **对应**：plan §5 / FR-002, SC-003, SC-004, SC-005
